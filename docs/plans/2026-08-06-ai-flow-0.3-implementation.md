# AI Flow 0.3 Implementation Plan

1. Add failing tests for specification hashing and linting.
2. Add failing tests for execution leases and conflict interaction.
3. Add failing tests for review bundle generation.
4. Add failing tests for progressive context budgets.
5. Implement deterministic modules and integrate them with workflow, presentation, CLI, config, and skills.
6. Run source and installed-package acceptance tests.
7. Package and checksum the exact validated archive.
