---
name: ai-flow
description: Automatically use for repository delivery requests to create, change, fix, redesign, implement, execute, continue, review, or validate frontend, backend, architecture, database, bugs, features, refactors, and design—including Spanish crea, cambia, corrige, rediseña, implementa, ejecuta, continúa, retoma, revisa, or valida. Routes new work explicitly; do not use for read-only explanations or research.
---
# AI Flow
Do not require the user to mention AI Flow, a skill name, a task ID, or a CLI command, and do not expose internal CLI instructions to the user.

## Entry gate and invariants
For every new delivery work item, choose the process route before any SpecRail task, CodeGraph preflight, gate, evidence state, or learning exists. Unless this is explicit `Continue/Resume/Retoma TASK-####` or a route prefix, use native `request_user_input` with `SpecRail`, `Directo`, `Directo + verificar`, and `Other`/free text. Never select a route for the user.
`Sin SpecRail:` / `No SpecRail:` means bypass SpecRail completely **as workflow state only**: no SpecRail CLI, task, CodeGraph preflight, gate, evidence state, or learning. `Directo + verificar:` / `Direct + Verify:` adds the smallest meaningful final verification. `SpecRail Fast:` selects governed SpecRail and uses `--mode fast`. Never infer total bypass from impatience or wording without an explicit route. Process route is independent of `micro/light/standard/rigorous` and Guided/Autonomous/Headless.
For every code-writing route require the official Ponytail capability in literal `full` mode before mutation. Missing/off/lite/imitated Ponytail blocks mutation; tell the user how to enable/install the official capability, never install third-party code silently, and never offer or accept a per-work-item Ponytail bypass. Before a mutation phase completes, run official `ponytail-review` against the diff. User requirements, security/privacy, accessibility, data-loss protection, approved scope, acceptance criteria, and evidence outrank minimalism.
Never fill a material unknown with model confidence. Resolve it from explicit active user input, an approved decision, an authoritative repository contract, one unique established repository pattern, or current deterministic tool evidence; otherwise ask 2–4 concrete choices, at most one recommendation, plus free-text `Other`. Recommendations are never preselected. Never print option lists or multiple-choice questions as text when native input is available.

## Host adapters
- **Codex:** use native `request_user_input`; only after SpecRail is selected use the managed/global `specrail` launcher. Codex owns model/reasoning. `$visualize` is optional and only valid when the exact installed skill is attested.
- **Pi:** use `specrail_entry_gate` for unresolved new-work routing; after SpecRail selection use `specrail_cli`, exact `specrail_host_context`, `specrail_skill`, and `specrail_codegraph`. Prefer an attested richer ask-user capability, otherwise forward the exact request-user-input payload through the Pi adapter. Pi owns model/thinking. `/specrail-handoff TASK-####` or `/new` + `Continue TASK-####` provides a fresh boundary. Unattested subagents/visualization use serial/canonical fallbacks.
- Without an interactive UI, never emulate a process route or human decision in text; stop unless explicit authority already exists.

## Deterministic control
1. Resolve existing work with `ai-flow resolve` by ID, exact title, or unique phrase. Task Markdown is the source of truth. Ambiguity uses the native task selector; never guess or create duplicate intake. Show `TASK-#### — Title`, easy to reference in another chat by ID, title, or descriptive phrase.
2. Let SpecRail own CodeGraph lifecycle. Do not manually run `codegraph init`, `sync`, `status`, or `index` during healthy task routing. Cached contract/health state and the active CodeGraph host transport are authoritative; full reindex is an explicit Doctor repair only. Fast micro/light may defer project-wide CodeGraph until escalation.
3. Read `next` first. If `specrail next` fails, treat it as a runtime blocker; `readiness` and `why-blocked` are diagnostics and never replace routing authority. Load only the returned specialist: on Pi call `specrail_skill`; on managed Codex read `.agents/skills/<recommendedSkill>/SKILL.md`. Do not rely on automatic mid-turn skill reselection and do not keep unrelated skills in context.
3a. Read `next.intelligence` as the **current-step host-agnostic capability recommendation** and `next.phaseIntelligence` as the capability recommendation for the active agent phase when the current step is only a human/deterministic boundary. `executor` is the default and owns local implementation reasoning; `frontier` is reserved for the compact material decision that has expected marginal value over the executor; `none` means the immediate step is deterministic/human-owned. Never claim that SpecRail changed the actual model: Codex/Pi/the host owns selection. Never spend frontier reasoning merely because a task is standard/rigorous/large, never replay the full chat into a frontier pass, and never use frontier to pre-write a step-by-step Builder plan. A frontier pass returns only a compact decision/constraints/facts/stop-condition capsule, then execution returns to the executor tier.
4. Product Owner and Target Audience opinions remain structured advisory inputs. Guided asks for acknowledgement; Autonomous/Headless may advance only mechanically safe decisions. A material revise/reject judgment remains human-owned.
5. Bounded post-approval feedback defaults to the same task as `REV-*`; do not regenerate the workflow or create a new task. Implement the small revision first, gather invalidated evidence, then decide permanent regression coverage after behavior stabilizes. Escalate only governed product/architecture/migration/security/breaking-scope changes.
6. Explicit close/finish-anyway or skip requests are governance input only for the deterministic close override or a registry-marked waivable workflow step. They never disable Ponytail, security/data-loss protections, or fabricate passed evidence.
7. Use one stable host session token for every human gate and obey `resolve-task-lease`. Respect the approved specification hash, immutable QA Mission hash, dependencies, repair budget, evidence gates, and `context.policy`; keep context small.

## Approval and presentation
At a human gate, never construct or paraphrase your own `request_user_input`. Use the exact `next.interaction` / `interaction` returned by SpecRail. When `interaction.tool === "host_actions"`, render the complete `interaction.presentation.markdown` Decision Capsule and execute only returned actions; there is no user decision yet. The complete authoritative Review Bundle remains the `REVIEW-BUNDLE` attachment/Review Details and is available on demand, not dumped into normal chat.
Every `present-image` action must surface the canonical attachment inside the conversation. Opening a file picker, printing a path, or naming Before/Proposal/After does not count. Record only actual action outcomes with stable session, current `presentationDigest`, and exact action ID. Required image failure blocks with `PRESENTATION_SURFACE_UNAVAILABLE`. Do not ask for approval until SpecRail returns `interaction.tool === "request_user_input"`.
Treat Review Cockpit generation and host presentation as different states. `specrail review cockpit` prepares HTML only; never claim it is open, rendered, attached, or visible without a concrete host result. The mandatory fallback remains every `requiredVisible` image in-conversation plus the `REVIEW-COCKPIT` open action. `artifactPrepared` and `referencePrepared` are not proof of display. A stale digest, changed evidence, different session, or tampered acknowledgement forces presentation again.
Normal output is delta-first: at most four short bullets (`%`, implemented, validated, blocker/next) unless the user asks for detail. Blockers, failed ACs, security/data-loss risk, scope violations, and stale/tampered evidence override brevity.

## Phase boundaries and delivery
A phase-boundary native choice is persisted, then the assistant turn ends. On the next turn enter the sealed boundary before implementation/review. Fresh chat is recommended only when runtime says so; same-chat entry is valid after the logical authority reset. Read the compiled handoff before editing and do not copy the full prior chat. When a boundary separates a frontier decision phase from an executor-owned phase, use `next.phaseIntelligence` to explain the upcoming capability tier; use the existing pause/change-model choice if the user wants to align the host selector. Do not require a model change when the host cannot attest one and never fake an automatic switch.
Done requires final approval and explicit delivery/merge. Use `delivery merge` or externally confirmed delivery; never equate implementation with delivery.

## SpecRail Fast
`SpecRail Fast:` is a shorter governed workflow, not a blind bypass. Micro/light defers project-wide Product Intelligence/CodeGraph and unnecessary agents, while preserving exact blast radius, stable ACs, real proof, Scope Guard, Acceptance Coverage, Ponytail full/review, and final approval. If deterministic classification reaches standard/rigorous, the normal 0.10 workflow resumes.

## Frontend and Visualize
Frontend work declares exact `Route → Target → pixel Viewport → Capture`. The primary screenshot must focus the exact task target; page-top/full-page captures are supplementary. Proposals use the installed Taste Skill workflow and Image Gen where applicable; final evidence uses the real app and browser/DevTools.
Serve previews over `http://` or `https://`; never use raw `index.html` or filesystem paths as website previews. Never use `file://` previews. If `presentation.previewUrl` exists, use it. Shell localhost failure is not Browser failure. Canonical generated image evidence uses `data:image` / data URI bytes and puts `data-specrail-evidence-id` directly on the `<img>`; unresolved local paths produce broken evidence and are invalid.
Discover the exact installed `visualize` skill from the Codex skill catalog. Do not ask the user to type `/visualize` and do not call `render.py` manually. Invoke `$visualize` only from a signed plan when it materially improves review. A valid Visual Comparator v2 keeps exact route/target/viewport/capture context, canonical evidence IDs and hashes, and interactive side-by-side/slider/overlay controls. Emit the native `visualize` content reference; generated HTML alone is not presentation. Record `rendered` only for validated artifact/reference preparation, while hostPresentation remains unverified and fallback remains required.

## Workflow invariants
- Product Specifier removes material ambiguity, writes executable QA Mission, stable `AC-*`, and risk-selected evidence. The deterministic specification linter must pass; approval hashes the governed specification.
- Final approval requires canonical Acceptance Coverage and clean Scope Guard. Governed scope changes use an Amendment; bounded `REV-*` does not rewrite the approved base specification.
- Do not spawn subagents for trivial work. Outside a deterministic concurrency plan, use at most three read-only subagents for useful parallel analysis; conflicting write lanes never run together.
- Repair loops are finite. Required tests/evals/evidence stay risk-selected and deterministic. Store traces/metrics locally only; no external telemetry.
- Environment repair uses `doctor --fix` as a plan and never silently installs Node, Git, CodeGraph, MCP, host plugins, or Ponytail.
