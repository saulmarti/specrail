import type { QualityPolicy, TaskDocument } from './types.js';
export function qualityPolicy(task:TaskDocument):QualityPolicy{const risk=String(task.meta.risk).toLowerCase(),size=String(task.meta.size).toLowerCase(),algorithmic=task.meta.surfaces.some(x=>['backend','data','database','algorithm'].includes(x));const reasons:string[]=[];let propertyTesting:QualityPolicy['propertyTesting']='none',mutationTesting:QualityPolicy['mutationTesting']='none';if(['critical','high'].includes(risk)){propertyTesting='required';mutationTesting='required';reasons.push('High-risk behavior requires independent failure-oriented tests.');}else if(risk==='medium'&&algorithmic){propertyTesting='recommended';mutationTesting=size==='large'?'recommended':'none';reasons.push('Data or backend behavior benefits from generated cases.');}return{propertyTesting,mutationTesting,reasons};}
export function applyQualityPolicy(task:TaskDocument):TaskDocument{const q=qualityPolicy(task);task.meta.route.property_testing=q.propertyTesting;task.meta.route.mutation_testing=q.mutationTesting==='required'||q.mutationTesting==='recommended';return task;}
