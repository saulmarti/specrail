import type { OperationalPolicy, TaskDocument } from './types.js';
export function operationalPolicy(task:TaskDocument):OperationalPolicy{const risk=String(task.meta.risk).toLowerCase(),ops=task.meta.surfaces.some(x=>['backend','api','infrastructure','performance','data','database'].includes(x));if(!ops)return{level:'none',requiredEvidence:[],reasons:[]};if(['critical','high'].includes(risk)||task.meta.surfaces.includes('performance'))return{level:'full',requiredEvidence:['operational-log','operational-trace','operational-metrics'],reasons:['High-risk operational behavior needs logs, traces, and metrics.']};if(risk==='medium')return{level:'focused',requiredEvidence:['operational-log','operational-metrics'],reasons:['Medium-risk backend behavior needs observable execution evidence.']};return{level:'none',requiredEvidence:[],reasons:[]};}
export function applyOperationalPolicy(task:TaskDocument):TaskDocument{task.meta.route.observability=operationalPolicy(task).level;return task;}
