name: Brain Workers diagnostic

on:
  push:
    branches:
      - 'feat/brain-workers'
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  isolate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm
      - name: Install
        run: npm ci --no-audit --no-fund
      - name: Build
        run: npm run build
      - name: Unit group I-L
        if: always()
        shell: bash
        run: |
          files=()
          for f in tests/[i-l]*.test.mjs; do
            [[ "$f" == "tests/installed-e2e.test.mjs" ]] || files+=("$f")
          done
          node --test --test-force-exit "${files[@]}"
      - name: Unit group M-P
        if: always()
        shell: bash
        run: |
          files=(tests/[m-p]*.test.mjs)
          node --test --test-force-exit "${files[@]}"
      - name: Unit group Q-T
        if: always()
        shell: bash
        run: |
          files=(tests/[q-t]*.test.mjs)
          node --test --test-force-exit "${files[@]}"
      - name: Unit group U-Z
        if: always()
        shell: bash
        run: |
          files=(tests/[u-z]*.test.mjs)
          node --test --test-force-exit "${files[@]}"
